# ── Runtime dependencies ────────────────────────────────────────────────────
#
# All versions are pinned with == (CWE-1104: supply-chain risk).
# To upgrade a package:
#   1. Review the changelog for the target version.
#   2. Update the pin below.
#   3. Run the full test suite: python -m unittest discover tests/
#   4. Commit the updated requirements.txt together with the code change.
#
# yt-dlp releases frequently and executes third-party network content —
# treat every upgrade as a security review, not a routine bump.

python-vlc==3.0.21203
yt-dlp==2026.3.17
Pillow==12.1.0
